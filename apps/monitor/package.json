{
  "name": "@repo/monitor",
  "private": true,
  "type": "module",
  "exports": {
    "./router": {
      "types": "./src/router.ts",
      "import": "./src/router.ts"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile=dist/monitor --compile --minify",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts --ignore-pattern src/old.ts"
  },
  "dependencies": {
    "@trpc/server": "^11.12.0",
    "bun-pty": "^0.4.8",
    "trpc-bun-adapter": "^1.3.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  }
}
