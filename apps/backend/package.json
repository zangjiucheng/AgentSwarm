{
  "name": "@repo/backend",
  "private": true,
  "type": "module",
  "exports": {
    "./router": {
      "types": "./src/router.ts",
      "import": "./src/router.ts"
    }
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun --sourcemap --external cpu-features",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@trpc/server": "^11.12.0",
    "@types/dockerode": "^4.0.1",
    "dockerode": "^4.0.9",
    "trpc-bun-adapter": "^1.3.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  }
}
